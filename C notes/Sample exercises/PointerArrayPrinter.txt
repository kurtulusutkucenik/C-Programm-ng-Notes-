#include <stdio.h>

// Program to print array elements sequentially using a pointer

int main(){
    
    int array[5] = {10,20,30,40,50};
    int *p = array;
    int i;
    
    for(i = 0; i < 5; i++){
        printf("*(p + %d) = %d\n", i, *(p+i));
    }
    
    return 0;
}